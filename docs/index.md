# My Embedded Book

Welcome! This site is built with **MkDocs + Material** and published via **GitHub Pages**.

!!! tip
    Use the left sidebar to navigate. Try the code copy button on code blocks.

```c
#include <stdio.h>
int main(void){ printf("Hello, MkDocs!\n"); }


# My Embedded Book

Welcome! This site is built with **MkDocs + Material** and published via **GitHub Pages**.

!!! tip
    Use the left sidebar to navigate. Try the code copy button on code blocks.

```c
#include <stdio.h>
int main(void){ printf("Hello, MkDocs!\n"); }

# Diagram (Mermaid)

```mermaid
Copy code
flowchart LR
  A[Idea] --> B[Design]
  B --> C[Implement]
  C --> D[Test]
  D -->|iterate| B
```
